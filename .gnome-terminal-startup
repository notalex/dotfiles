# vim: set ft=sh:

if [ $SETUP_NOTES_WINDOW ]; then
  i3 move down
  i3 border none

  for i in 1 2 3; do
    i3 resize shrink up
  done

  if $(tmux has-session -t notes); then
    tmx-attach-session notes
  else
    automux notes
  fi # rvm is not detected on startup
fi
