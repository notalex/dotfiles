#! /bin/bash

sleep 20
guake &

sleep 2
guake -e 'tmx -g'

sleep 2
guake --new-tab=1
guake --select-tab=1
command_path="/home/alex/bin/guake-tab-1-command"
[ -s "$command_path" ] && guake -e "$(cat $command_path)"
