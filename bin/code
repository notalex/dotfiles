#! /usr/bin/env bash

# Target the latest compiled `code` binary.
target_root="$HOME/projects/code/.code/working/_target-cache/code"
latest_dir=$(ls -1dt "$target_root"/* 2>/dev/null | head -n 1)

exec "$latest_dir/code-rs/dev-fast/code" "$@"
