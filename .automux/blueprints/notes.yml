name: notes
flags: -u2
hooks:
  post:
    - <%= select_window 2 %>
windows:
  - index: 2
    panes:
      - vim home_work.notes
    hooks:
      pre: cd ~/gitbasket
  - index: 3
    panes: vim ~/gitbasket/history.txt
  - index: 7
