name: small
root: '-r:'
flags: -u2
hooks:
  post: <%= select_window 2 %>
windows:
  - panes:
  - panes: nvim
    index: 2
  - panes: cisco-anyconnect connect vpn-dc1.blackducksoftware.com
    index: 1
  - hooks:
      pre: git pull origin master
    root: ~/gitbasket
    index: 4
    panes: nvim WorkFlow.notes
  - index: 7
    hooks:
      pre: git pull origin master
