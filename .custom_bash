# vim: set ft=sh:

. ~/.bash_prompt

stty stop undef
stty eof ^o

declare -A KEY
KEY=(
		[CTRL]="\C"
		[META]="\e"
		[ENTER]="\n"
    )

git_status="\"${KEY[CTRL]}-s\":\"gs${KEY[ENTER]}\""

bind $git_status
