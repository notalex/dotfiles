startup_message off

# ctrl key mappings can only be done with bindkey
bindkey ^s eval copy "stuff ?"  
bindkey ^d eval meta colon "stuff 'echo stubbed'" 'stuff \015'

vbell off                       # Turns visual bell off
bind z  # unbind suspend
bind d  # unbind detach

# rebind kill
bind k  
bind Q kill

# meta invokes command (^a)  # \015 = ^M = Enter key
bind r eval meta colon "stuff 'source ~/.screenrc'" "stuff ^M"; eval meta colon "stuff 'echo screen_reloaded'" 'stuff \015'
bind . eval meta colon "stuff 'number '"
bind ; eval meta colon "stuff 'resize '"

# rebind rename tab
bind A
bind < title

